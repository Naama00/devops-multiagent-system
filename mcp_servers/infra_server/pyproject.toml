[project]
name = "infra-mcp-server"
version = "0.1.0"
description = "MCP Infrastructure Server for DevOps Multi-Agent System"
dependencies = [
    "mcp>=1.0.0",
    "pydantic>=2.0.0"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"